# Reports

This directory will hold the reproducible technical report, generated figures, generated tables, and final PDF outputs.

Quarto and LaTeX/TinyTeX are expected system-level reporting tools for the final report. They are documented here rather than installed as Python dependencies.
