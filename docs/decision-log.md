# Decision Log

| Date | Decision | Alternatives | Rationale | Evidence | Consequences | Revisit condition |
|---|---|---|---|---|---|---|
| 2026-06-09 | Use `pyproject.toml` plus committed `uv.lock` as the canonical environment. | `pyproject.toml` without lockfile; another package manager. | Matches README, PRD, and reproducibility goals. | `docs/PRD.md` technical stack and M0 planning decision. | `uv sync --locked` becomes the CI setup path. | Revisit only if required dependencies cannot support Python 3.11 through `uv`. |
| 2026-06-09 | Keep M0 workflow commands as successful documented placeholders. | Fail-fast stubs; full CLI implementation. | Lets README commands resolve without starting data or model work too early. | M0 plan and roadmap objective. | Later milestones must replace placeholders with real implementations. | Revisit when each milestone begins implementation. |
| 2026-06-09 | Run lint, formatting, typing, tests, importability, and CLI smoke checks in CI. | Lint/tests only; import-only CI. | Matches PRD recommendation without requiring raw data. | `docs/PRD.md` CI scope and M0 planning decision. | CI stays data-free until later smoke fixtures exist. | Revisit when a synthetic pipeline smoke test is implemented. |
| 2026-06-09 | Copy the local `KSAS-Dataset/` into `data/raw/KSAS-Dataset/` before deleting the root copy. | Move the root dataset directly. | Copy-first keeps a rollback source until the audit and ignore checks pass. | `git check-ignore -v` confirms both raw locations are ignored; `hmc audit` passes. | The root ignored copy can be deleted manually after confirming the copied raw path is sufficient. | Revisit only if local storage becomes a problem or the copied dataset is corrupted. |
| 2026-06-09 | Generate `data/manifests/samples.csv` from `hmc audit` rather than editing it manually. | Hand-authored manifest. | Generated manifests are reproducible and can enforce file, schema, label, participant, arm, checksum, and quality invariants. | `results/audit/ksas_audit_summary.json` reports 240 valid files and no audit errors. | Future preprocessing must use the manifest as source of truth. | Revisit if the dataset structure changes. |
| 2026-06-09 | Treat KSAS sampling frequency as unknown in M1. | Infer Hz from sequence lengths or unstated assumptions. | The README and CSVs provide no timestamps or sampling-rate value. | `docs/data-dictionary.md`, `docs/open-questions.md`, and `data/manifests/ksas_provenance.json`. | Temporal spans are reported in samples; seconds-based interpretation is deferred. | Revisit if credible protocol documentation or timestamp evidence is found. |
